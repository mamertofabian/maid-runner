interface:
  display_name: "MAID Outcome Enrich"
  short_description: "Generate advisory Outcome enrichment outside maid-runner"
  default_prompt: "Use $maid-outcome-enrich to run `maid enrich prompt`, generate a candidate digest with local llama-server by default, disclose CLOUD-PRIVACY before any explicit cloud opt-in, treat `maid enrich validate` failures as hard stops that require regeneration, and render the validated digest with `maid enrich render`."
