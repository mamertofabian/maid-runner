version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/reference/conf.py

python:
  install:
    - requirements: docs/reference/requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - all
