# TODO

- Add support for types in the manifest and validations
- Add more ast_validator tests but in a separate test file
- Optionally create two new test files that splits the test_ast_validator.py without deleting it until the split is properly verified.
