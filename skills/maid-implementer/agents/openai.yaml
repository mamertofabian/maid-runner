interface:
  display_name: "MAID Implementer"
  short_description: "Implement an approved MAID manifest without drifting scope"
  default_prompt: "Use $maid-implementer to read the approved manifest, load only declared files, implement the behavior required by the tests, and validate both implementation and integration before finishing."
