schema: '2'
goal: Add `maid incident export --format dpo` emitting JSONL chosen/rejected training pairs
type: feature
created: '2026-06-15T05:11:42Z'
metadata:
  epic: 071-00-gaming-incident-flywheel.epic
  tags:
  - anti-gaming
  - incidents
  - dpo-data
  priority: low
description: |
  Implement the training-data export over the incident store created by
  071-01: `maid incident export --format dpo --output <path>`.

  Depends on 071-01 (record format, closed vocabulary, and the
  `maid incident` command surface in `maid_runner/core/incidents.py` and
  `maid_runner/cli/commands/incident.py`). Do not promote before 071-01;
  children of this epic land strictly in order.

  Pinned decisions (do not redesign):

  1. CLI surface, exactly: `maid incident export --format dpo --output
     <path>`. It emits a JSONL file where each line pairs `rejected_diff`
     against `chosen_diff` with the packet diagnostics as context.
  2. Records without a chosen diff are SKIPPED and the skip count is
     reported. The export is a dumb serializer; no filtering heuristics
     beyond completeness (a record is complete when it has both
     rejected_diff and chosen_diff).
  3. Non-AI contract: export serializes only explicit stored data. No
     inference, scoring, deduplication, or quality heuristics.
  4. Output is deterministic: lines are ordered by the same
     timestamp-then-filename ordering that `maid incident list` uses, and
     each line is JSON with stable key order.

  Behavioral test expectations (from the epic): JSONL line shape,
  incomplete-record skipping with reported counts, deterministic ordering,
  and empty store behavior (an empty or absent incident store produces an
  output file with zero lines, reports zero exported and zero skipped, and
  exits zero).

  Edge cases: an unknown `--format` value is a usage error naming the
  supported format; a missing `--output` is a usage error; a malformed
  incident record file fails the export loudly with the record path named
  rather than being silently skipped (skipping is only for structurally
  valid records lacking chosen_diff).

  Planning note: the epic pins the pairing and context but not the exact
  JSONL keys. Conservative reading: each line is an object with exactly
  `context` (the packet `diagnostics` list embedded in the record),
  `rejected` (rejected_diff text), and `chosen` (chosen_diff text). No
  extra fields are invented.

  Out of scope for this child: record capture/update/list (071-01),
  temptation suggestions (071-03), and skill guidance (071-04).
temptations:
- risk: Do not add filtering, scoring, or deduplication heuristics to the export.
  instead: Serialize every complete record deterministically; the only exclusion rule is missing chosen_diff, reported as a skip count.
- risk: Do not silently drop malformed records to keep the export running.
  instead: Fail loudly with the malformed record path; skipping is reserved for valid records that lack a chosen diff.
- risk: Do not embed full repository state or extra invented fields in export lines.
  instead: Emit exactly the packet diagnostics as context plus the rejected and chosen diffs, keeping lines prompt-sized and reviewable.
files:
  create:
  - path: tests/core/test_incident_export.py
    artifacts:
    - kind: test_function
      name: test_export_incidents_dpo_writes_jsonl_context_rejected_and_chosen
    - kind: test_function
      name: test_export_incidents_dpo_skips_valid_records_without_chosen_diff
    - kind: test_function
      name: test_export_incidents_dpo_orders_lines_like_incident_listing
    - kind: test_function
      name: test_export_incidents_dpo_empty_or_absent_store_writes_zero_lines
    - kind: test_function
      name: test_export_incidents_dpo_names_malformed_record_path
  - path: tests/cli/test_incident_export_cmd.py
    artifacts:
    - kind: test_function
      name: test_build_parser_registers_incident_export_options
    - kind: test_function
      name: test_incident_export_writes_dpo_jsonl_and_reports_counts
    - kind: test_function
      name: test_incident_export_empty_store_reports_zero_counts
    - kind: test_function
      name: test_incident_export_malformed_record_returns_error_with_path
    - kind: test_function
      name: test_incident_export_rejects_unknown_format_and_missing_output
  edit:
  - path: maid_runner/core/incidents.py
    artifacts:
    - kind: class
      name: DpoExportReport
      description: Deterministic export result with exported and skipped counts.
    - kind: attribute
      name: exported_count
      of: DpoExportReport
      type: int
    - kind: attribute
      name: skipped_count
      of: DpoExportReport
      type: int
    - kind: attribute
      name: output_path
      of: DpoExportReport
      type: str
    - kind: function
      name: export_incidents_dpo
      args:
      - name: incidents_dir
        type: Union[str, Path]
      - name: output_path
        type: Union[str, Path]
      returns: DpoExportReport
      source_scenario: An incident store contains complete records, records without a chosen diff, and is exported to JSONL.
      actions:
      - type: api_call
        subject:
          function: export_incidents_dpo
      expected:
        behavior: Each complete record becomes one JSONL line pairing rejected against chosen with packet diagnostics as context; records without chosen_diff are skipped and counted; ordering matches list ordering; an empty store yields zero lines and zero counts; malformed record files fail loudly.
  - path: maid_runner/cli/commands/incident.py
    artifacts:
    - kind: function
      name: cmd_incident
      args:
      - name: args
        type: argparse.Namespace
      returns: int
      source_scenario: '`maid incident export --format dpo --output pairs.jsonl` serializes the incident store.'
      actions:
      - type: api_call
        subject:
          function: cmd_incident
      expected:
        behavior: export writes the JSONL file, reports exported and skipped counts, rejects unknown formats and missing output paths as usage errors, and returns nonzero for malformed record files.
  - path: maid_runner/cli/commands/_main.py
    artifacts:
    - kind: function
      name: build_parser
      args: []
      returns: argparse.ArgumentParser
      source_scenario: A user invokes `maid incident export --format dpo --output <path>`.
      actions:
      - type: api_call
        subject:
          function: build_parser
      expected:
        behavior: The incident subcommand exposes an export action with --format and --output options.
  read:
  - manifests/drafts/071-00-gaming-incident-flywheel.epic.yaml
  - manifests/drafts/070-00-structured-retry-packets.epic.yaml
  - manifests/drafts/071-01-add-incident-record-storage-and-capture.manifest.yaml
validate:
- uv run python -m pytest -q tests/core/test_incident_export.py tests/cli/test_incident_export_cmd.py
outcome:
  status: completed
  summary: "Added deterministic DPO JSONL export for stored gaming incidents and wired `maid incident export --format dpo --output <path>`."
  rationale: "Complete incident records now serialize to stable-key JSONL objects containing exactly context, rejected, and chosen; structurally valid records without chosen_diff are skipped and counted; empty stores write an empty file; malformed incident records fail loudly with the record path named. The active manifest was corrected to declare the new behavioral test files as created artifacts, then the plan lock was revised with preserved red evidence so strict worktree and plan-lock gates pass without fabricating post-implementation red evidence."
  lessons:
  - lesson_type: workflow
    summary: "When a promoted draft creates new behavioral tests, those test files must be writable manifest scope, not only files.read, or strict worktree_scope verify correctly blocks handoff."
    tags:
    - plan-lock
    - worktree-scope
    - behavioral-tests
    paths:
    - manifests/071-02-add-dpo-export.manifest.yaml
    - tests/core/test_incident_export.py
    - tests/cli/test_incident_export_cmd.py
  - lesson_type: implementation
    summary: "DPO export remains a serializer over explicit incident data: diagnostics context plus rejected and chosen diffs, with no scoring, filtering heuristics, deduplication, or invented fields."
    tags:
    - incidents
    - dpo-data
    - anti-gaming
    paths:
    - maid_runner/core/incidents.py
    - tests/core/test_incident_export.py
  review_notes:
  - source: implementation-review-subagent
    severity: ready
    summary: "Read-only reviewer Pasteur reported no findings; scope matched the active manifest, behavioral tests covered the contract, the stale draft reference was only in future 071-04 scope, and the final verdict was Ready to merge."
  validation:
  - command:
    - uv
    - run
    - python
    - -m
    - pytest
    - -q
    - tests/core/test_incident_export.py
    - tests/cli/test_incident_export_cmd.py
    status: passed
    summary: "Focused incident export tests passed: 10 passed."
  - command:
    - uv
    - run
    - maid
    - validate
    - manifests/071-02-add-dpo-export.manifest.yaml
    - --mode
    - behavioral
    - --packet
    status: passed
    summary: "Promoted manifest behavioral validation passed with existing chain warnings."
  - command:
    - uv
    - run
    - maid
    - validate
    - manifests/071-02-add-dpo-export.manifest.yaml
    - --mode
    - implementation
    - --packet
    status: passed
    summary: "Promoted manifest implementation validation passed with existing chain warnings."
  - command:
    - uv
    - run
    - maid
    - test
    - --manifest
    - manifests/071-02-add-dpo-export.manifest.yaml
    status: passed
    summary: "Manifest test command passed: 1 command passed, 0 failed."
  - command:
    - uv
    - run
    - maid
    - validate
    - --packet
    status: passed
    summary: "Repository validation passed: 334 manifests, 248 passed, 0 failed, 86 skipped; existing chain issues remain."
  - command:
    - uv
    - run
    - maid
    - test
    status: passed
    summary: "Repository manifest command suite passed: 99 commands passed, 0 failed; existing chain issues remain."
  - command:
    - uv
    - run
    - ruff
    - check
    - maid_runner/core/incidents.py
    - maid_runner/cli/commands/incident.py
    - maid_runner/cli/commands/_main.py
    - tests/core/test_incident_export.py
    - tests/cli/test_incident_export_cmd.py
    status: passed
    summary: "Ruff passed on touched Python files."
  - command:
    - uv
    - run
    - maid
    - verify
    - --require-plan-lock
    - --require-red-evidence
    - --since
    - HEAD
    - --include-tests
    - --packet
    status: passed
    summary: "Strict handoff gate passed: schema, behavioral, coherence, file_tracking, plan_lock, worktree_scope, changed_scope, and tests all passed."
  completed_at: '2026-06-15T05:27:32Z'
