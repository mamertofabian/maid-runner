{
    "goal": "Implement the `validate_with_ast` function in the existing manifest_validator.py file. This function will parse a given Python test file into an Abstract Syntax Tree (AST) to verify that the artifacts listed in `expectedArtifacts` are actually referenced in the code. If an expected artifact is not found, it must raise a custom `AlignmentError`.",
    "editableFiles": [
      "validators/manifest_validator.py"
    ],
    "readonlyFiles": [
      "tests/test_ast_validator.py"
    ],
    "expectedArtifacts": {
      "file": "validators/manifest_validator.py",
      "contains": [
        {
          "type": "class",
          "name": "AlignmentError",
          "base": "Exception"
        },
        {
          "type": "function",
          "name": "validate_with_ast",
          "parameters": ["manifest_data", "test_file_path", "use_manifest_chain"]
        }
      ]
    },
    "validationCommand": "pytest tests/test_ast_validator.py"
  }