# archive-kind: consumed-draft-epic
schema: "2"
goal: "Archive consumed MAID Runner performance roadmap epic"
type: refactor
created: "2026-05-29"
metadata:
  status: archived
  tags:
    - performance
    - efficiency
    - caching
    - parser-reuse
    - archive
  priority: low
description: |
  This split-before-promote epic has been consumed. Its implementation-sized
  child contracts were promoted as `034-01` through `034-03`.

  The path is still read by active manifests as historical context, so it
  remains stable as an archived pointer instead of being deleted. Current
  performance priorities live in `docs/plans/maid-runner-performance-backlog.md`.
files:
  edit:
    - path: manifests/drafts/034-00-maid-runner-performance-roadmap.epic.yaml
      artifacts:
        - kind: attribute
          name: _archived_performance_roadmap_epic
          of: manifest
          description: "Historical pointer for promoted child manifests 034-01 through 034-03."
  read:
    - docs/plans/maid-runner-performance-backlog.md
    - manifests/034-01-cache-manifest-chain-per-cli-invocation.manifest.yaml
    - manifests/034-02-share-source-ast-across-validation-phases.manifest.yaml
    - manifests/034-03-cache-test-artifact-tables-across-manifests.manifest.yaml
validate:
  - uv run maid validate manifests/drafts/034-00-maid-runner-performance-roadmap.epic.yaml --mode schema --quiet
