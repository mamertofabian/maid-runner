# archive-kind: abandoned-draft-manifest
schema: "2"
goal: "Archive abandoned daemon auto-default flip draft"
type: feature
created: "2026-06-18T02:30:00Z"
metadata:
  status: archived
  epic: 064-00-daemon-first-agent-validation
  tags:
    - performance
    - daemon
    - maid-serve
    - agent-loops
    - archive
  priority: medium
description: |
  This default-flip draft was abandoned before implementation because it
  depended on the 064-01 CLI `--daemon auto|off|require` routing slice.
  The 064-01 slice was also abandoned before merge after targeted benchmarks
  showed practical parity rather than a measurable win.

  Future default-routing work must be planned as a new child after an
  integration path demonstrates a measurable win without per-call client CLI
  startup.
files:
  create:
    - path: manifests/drafts/064-05-benchmark-and-default-daemon-auto.manifest.yaml
      artifacts:
        - kind: attribute
          name: _abandoned_daemon_auto_default_flip
          of: manifest
          type: archived draft
          description: "Records that the daemon auto-default flip draft was abandoned after 064-01 failed its benchmark gate."
validate:
  - uv run maid validate manifests/drafts/064-05-benchmark-and-default-daemon-auto.manifest.yaml --mode schema --quiet
